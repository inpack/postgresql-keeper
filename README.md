## inPack for PostgreSQL

automated configuration management for PostgreSQL

* Needs Go 1.8+ (https://golang.org)

``` shell
yum install -y readline-devel zlib-devel

go install github.com/sysinner/inpack/cmd/inpack

inpack build
```
